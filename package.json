{
  "dependencies": {
    "dotenv": "^16.5.0",
    "mongoose": "^8.14.0",
    "react-router-dom": "^7.5.2",
    "vite": "^6.2.6"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build"
  }
}
